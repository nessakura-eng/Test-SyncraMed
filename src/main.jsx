import React from "react";
import ReactDOM from "react-dom/client";
import SyncraMedPreview from "./SyncraMedPreview.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SyncraMedPreview />
  </React.StrictMode>
);
